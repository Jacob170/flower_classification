import os
import numpy as np
from sklearn.model_selection import train_test_split
import shutil


def split_dataset(image_dir, labels, output_dir, split_num=1):
    """
    Split dataset into train (50%), validation (25%), test (25%)
    """
    # Your code to split data
    # Save splits to output_dir/split_{split_num}/train, val, test
    pass
